Currently inverting the final permutation and splitting halves to right16 and left16 are the only working points.


bitset<32> pPermutationInverse(bitset<32> input) {
    bitset<32> output;
    for (int i = 0; i < 32; ++i) {
        output[31 - i] = input[32 - P[i]];
    }
    return output;
}

^ will use?